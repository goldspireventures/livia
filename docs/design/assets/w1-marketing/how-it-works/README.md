# W1 How it works — Constellation inheritance (M4)

**Live:** `/how-it-works` · **Reference:** `m4-how-inherit.reference.png`

Inherits from home M4 without the full orbital diagram:

| Layer | What carries over |
|-------|-------------------|
| Ambient | Same page shell as pricing (stars, nebula, orbit watermark) |
| Typography | Eyebrow + serif title with champagne accent |
| Components | `ConstellationSpine` — gold vertical thread + star nodes at each chapter |
| Rhythm | Chapter index in cyan, body in muted white |

**Code:** `constellation-spine.tsx`, `how-it-works.tsx`

This is the “thread without the map” — same physics story, lighter weight than the hero.
