# Screen card PNG exports

**Purpose:** North-star PNGs traced from Figma frames — one file per `meta.id`.

**Naming:** `{meta.id}.png` (matches YAML filename, e.g. `w5.public.book.mobile.png`)

**Workflow:** [`FIGMA-SCREEN-MANIFEST.md`](../FIGMA-SCREEN-MANIFEST.md) §2

**Status:** 24/24 P0 exported (2026-05-31) — 14 northstar, 4 Figma wireframes, 6 fallback v1.

Do not commit placeholder PNGs — use inventory 📋 until real export lands.
