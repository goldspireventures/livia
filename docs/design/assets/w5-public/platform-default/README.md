# Platform Default — guest `/b` (W5)

**Preset:** `platform-default` (Constellation — same family as new signups)

| Surface | File |
|---------|------|
| Mobile book | `mobile/book-mobile.target.png` |

**Capture (Bloom demo):**

```bash
pnpm capture:platform-default-book
```

Preview URL: `/b/bloom-beauty-dublin?preview=1&preset=platform-default&vertical=beauty`

Runtime copy: `artifacts/livia-dashboard/public/w2-gateway/platform-default/book-mobile.png`
