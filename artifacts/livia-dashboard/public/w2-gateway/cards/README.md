# G1 trade-card hero photos

Bundled crops from the locked northstar `docs/design/assets/w2-gateway/demo/g1-wedge-web.target.png`.

Regenerate after mock updates:

```bash
python scripts/extract-g1-card-photos.py
```

Do not swap in random stock — warm trade photography must match the gold frame language in the mock.
