/** Fixed scroll viewport — 5 rows at 5.25rem each; row 6+ scrolls inside. */
export const bookingsListScrollViewportClass =
  "h-[calc(5.25rem*5)] shrink-0 overflow-y-auto overscroll-contain";

export const bookingsListRowHeightClass = "min-h-[5.25rem]";
